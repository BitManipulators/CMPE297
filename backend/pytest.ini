[pytest]
markers =
    unit: Unit tests (fast, isolated tests)
    integration: Integration tests (may require external services)
    slow: Slow running tests
    requires_credentials: Tests that require API credentials or external services

